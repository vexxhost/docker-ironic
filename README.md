# docker-ironic
Docker image for OpenStack Ironic

The image also installs the upstream Ironic Prometheus Exporter notification
driver and HTTP application. The exporter source is pinned in the build
workflow and validated during every image build.
